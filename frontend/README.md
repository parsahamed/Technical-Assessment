# Assessment Frontend

React + Vite + TypeScript. **This is where you implement the assessment tasks.**

## Run

```bash
npm install
npm run dev
```

App runs at http://localhost:5173. `/api` is proxied to the backend (must be running on port 3001).

See [ASSESSMENT.md](../ASSESSMENT.md) in the repo root for tasks and API reference.
